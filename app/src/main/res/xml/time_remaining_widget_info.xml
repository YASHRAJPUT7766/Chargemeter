<?xml version="1.0" encoding="utf-8"?>
<!--
    Same no-updatePeriodMillis-reliance rationale as
    charge_meter_widget_info.xml — ChargingMonitorService pushes explicit
    updates via TimeRemainingWidgetUpdater instead.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="140dp"
    android:minHeight="90dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="0"
    android:previewImage="@drawable/time_remaining_widget_preview"
    android:initialLayout="@layout/widget_loading"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/time_remaining_widget_description" />
